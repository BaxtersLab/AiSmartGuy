[package]
name = "manifest"
version = "0.1.0"
edition = "2021"

[dependencies]
utils        = { path = "../utils" }
logging      = { path = "../logging" }
error_system = { path = "../error_system" }
serde        = { version = "1", features = ["derive"] }
serde_json   = "1"
